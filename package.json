{
  "name": "eac",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "eac": "./dist/eac"
  },
  "scripts": {
    "build": "bun build src/cli.ts --compile --outfile dist/eac",
    "package:release": "bun run build && node scripts/package-release.mjs"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
